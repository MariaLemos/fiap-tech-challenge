export default function TransactionsPage() {
  return (
    <main className="grid gap-4 py-4">
      <section className="grid gap-4 rounded-md bg-foreground p-8 text-font">
        <span className="text-muted">Microfrontend / Transactions</span>
        <h1 className="text-2xl font-bold">Transactions</h1>
        <p>
          Estrutura reservada para a lista completa, busca, filtros,
          paginacao, criar/editar, validacao e upload simulado.
        </p>
      </section>
    </main>
  );
}
